import useTitle from "Model/View/Header/useTitle";

interface ScheduleHomeProps {}

const ScheduleHome: React.FC<ScheduleHomeProps> = ({}) => {
	useTitle("shedule home");
	return <div></div>;
};

export default ScheduleHome;
